pipeline {
   agent any

   stages {
      stage('Verify Branch') {
         steps {
            echo "$GIT_BRANCH"
         }
      }
      stage('HolaMundo') {
         steps {
            echo 'Hola mundo'
         }
      }
   }
}
