pipeline {
   agent any

   stages {
      stage('Verify Branch') {
         steps {
            echo "$GIT_BRANCH"
         }
      }
      stage('Docke Build') {
         steps {
            echo 'Hola mundo'
         }
      }
   }
}
